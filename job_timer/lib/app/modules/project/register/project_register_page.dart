import 'package:flutter/material.dart';

class ProjectRegisterPage extends StatelessWidget {
  const ProjectRegisterPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Project Register'),
      ),
      body: Container(),
    );
  }
}
